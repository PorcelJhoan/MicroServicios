spring:
  application:
    name: ms-config-server
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/PorcelJhoan/MicroServicios
          search-paths: config-data

server:
  port: 8888
